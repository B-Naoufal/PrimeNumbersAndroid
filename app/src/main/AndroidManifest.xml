<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.clubhayat.primenumbers">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Primeornot" />
        <activity android:name=".Divisors" />
        <activity android:name=".Primes" />
        <activity android:name=".Betweenthem" />
        <activity android:name=".Encrypt" />
        <activity android:name=".Decrypt"></activity>
    </application>

</manifest>